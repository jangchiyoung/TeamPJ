<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- sql mapper   -->  
<mapper namespace="Member"> <!-- namespace : 일종의 식별역할의 변수 -->

	<!-- 회원 가입 -->
	<insert id="insert" parameterType="Member" useGeneratedKeys="true"
				keyColumn="idx" keyProperty="idx">
		insert into member(member_id,member_password,member_name,member_img1,member_tel,member_email,member_address)
		values (#{member_id},#{member_password},#{member_name},#{member_img1},#{member_tel},#{member_email},#{member_address})
	</insert>
	
	<!-- 회원가입 완료 화면 -->
	<select id="getInfo" parameterType="String" resultType="Member">
		select * from member where member_id = #{member_id}
	</select>
	
	<!-- 로그인 체크 -->
	<select id="loginCheck" parameterType="java.util.Map" resultType="Member">
		select * from member where member_id = #{member_id} and member_password = #{member_password}  
	</select>
	
	<!-- 아이디 찾기 -->
	<select id="findId" parameterType="java.util.Map" resultType="Member">
		select * from member where member_name = #{member_name} and member_email = #{member_email} and member_tel = #{member_tel}
	</select>
	
	<!-- 비밀번호 찾기 -->
	<select id="findPassWord" parameterType="java.util.Map" resultType="Member">
		select * from member where member_id = #{member_id} and member_email = #{member_email} and member_tel = #{member_tel}
	</select>
	
	
	<!-- 비밀번호 수정 -->
	<update id="update_password" parameterType="java.util.Map">
		update member set member_password =#{member_password}
		where member_id =#{member_id}
	</update>
	
	<!-- 마이페이지 수정 -->
	<update id="update_myprofile" parameterType="Member">
		update member set 
		member_password = #{member_password}, 
		member_name = #{member_name}, 
		member_tel = #{member_tel}, 
		member_email = #{member_email}, 
		member_address = #{member_address}, 
		member_img1 = #{member_img1} 
		where member_id = #{member_id}
	</update>
	
	<!-- 아이디체크 -->
	<select id="idCheck" parameterType="String" resultType="int">
		select count(member_id) from member where member_id = #{member_id}
	</select>
	
	<!-- 사진 등록 -->
	<insert id="fileupload" parameterType="Gallery">
	insert into member(member_img) values(#{member_img}) where member_id = #{member_id}
	</insert>
</mapper>